/**
 * ===================================================================
 * HOOKS INDEX - Export tất cả hooks
 * ===================================================================
 */

// Media Query hooks (sử dụng matchMedia API)
export {
  useMediaQuery,
  useIsDesktop,
  useIsTablet,
  useIsMobile,
  BREAKPOINTS,
} from './useBreakpoint';
